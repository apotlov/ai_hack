# 🛡️ MVP Антифрод система для банка

Система машинного обучения для детекции банковского мошенничества, разработанная для хакатона.

## 📋 Описание

Антифрод система анализирует поведенческие данные пользователей (Amplitude) и аудио данные для выявления потенциально мошеннических операций. Система использует машинное обучение для классификации транзакций как легитимных или подозрительных.

## 🏗️ Архитектура системы

```
hackathon/
├── data/                          # Данные
│   ├── amplitude/                 # Parquet файлы с поведенческими данными
│   ├── audio/                     # WAV аудио файлы
│   └── targets.csv               # Целевые метки (fraud/not fraud)
├── src/                          # Исходный код
│   ├── data_loader.py            # Загрузка данных
│   ├── amplitude_processor.py    # Обработка Amplitude данных
│   ├── audio_processor.py        # Обработка аудио
│   ├── feature_extractor.py      # Объединение признаков
│   └── model_trainer.py          # ML модель
├── scripts/                      # Скрипты запуска
│   ├── main.py                   # Главный скрипт
│   ├── train.py                  # Обучение модели
│   └── predict.py                # Предсказания
├── models/                       # Сохраненные модели
├── output/                       # Результаты
└── requirements.txt              # Зависимости
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите ваши данные в соответствующие директории:
- **Amplitude данные**: `data/amplitude/*.parquet`
- **Аудио файлы**: `data/audio/*.wav`
- **Целевые метки**: `data/targets.csv` (колонки: user_id, is_fraud)

### 3. Запуск системы

#### Полный пайплайн (обучение + предсказание):
```bash
python scripts/main.py --full
```

#### Только обучение:
```bash
python scripts/main.py --train
```

#### Только предсказания:
```bash
python scripts/main.py --predict
```

#### Проверка системы:
```bash
python scripts/main.py --check
```

## 📊 Компоненты системы

### DataLoader
- Загружает данные из Parquet файлов (Amplitude)
- Обрабатывает аудио файлы в формате WAV
- Загружает целевые метки из CSV

### AmplitudeProcessor
Извлекает поведенческие признаки:
- **Временные**: частота активности, время между событиями
- **Поведенческие**: типы событий, продолжительность сессий
- **Активность**: паттерны по дням/часам

### AudioProcessor
Извлекает аудио признаки:
- **MFCC**: Mel-frequency cepstral coefficients
- **Спектральные**: центроид, полоса пропускания, roll-off
- **Временные**: RMS энергия, основная частота

### FeatureExtractor
- Объединяет признаки из всех источников
- Обрабатывает пропущенные значения
- Создает единый датасет для ML

### ModelTrainer
- Использует Random Forest для классификации
- Автоматическая предобработка данных
- Валидация и метрики качества

## 🎯 Результаты работы

После выполнения система создает:

### В директории `models/`:
- `antifraud_model_v1.joblib` - обученная модель
- `model_info.txt` - информация о модели и метриках
- `training_features.csv` - признаки обучающей выборки

### В директории `output/`:
- `fraud_predictions.csv` - детальные предсказания
- `prediction_summary.txt` - сводка результатов
- `recommendations.txt` - рекомендации по действиям

## 📈 Метрики качества

Система отслеживает следующие метрики:
- **AUC-ROC**: площадь под ROC-кривой
- **Precision**: точность детекции мошенничества
- **Recall**: полнота детекции мошенничества
- **F1-Score**: гармоническое среднее precision и recall

## 🔧 Конфигурация

### Основные параметры модели:
- **Random Forest**: n_estimators=100, max_depth=10
- **Частота дискретизации аудио**: 22050 Hz
- **MFCC коэффициентов**: 13
- **Размер тестовой выборки**: 20%

### Пороги риска:
- **Низкий риск**: < 30%
- **Средний риск**: 30-70%
- **Высокий риск**: > 70%

## 📋 Формат данных

### Amplitude данные (Parquet):
```
user_id | event_time | event_type | session_duration | click_count | page_views
user_1  | 2024-01-01 | login      | 300              | 10          | 5
```

### Целевые метки (CSV):
```
user_id,is_fraud
user_1,0
user_2,1
```

### Аудио файлы:
- Формат: WAV, MP3, FLAC
- Именование: `{user_id}_audio.wav`

## 🚨 Использование результатов

### Случаи высокого риска (≥70%):
1. **Немедленная проверка** транзакций
2. **Блокировка** подозрительных операций
3. **Контакт с клиентом** для подтверждения

### Случаи среднего риска (30-70%):
1. **Усиленный мониторинг**
2. **Дополнительные алерты**
3. **Периодический пересмотр**

## 🛠️ Разработка и отладка

### Создание тестовых данных:
```python
from src.feature_extractor import FeatureExtractor
extractor = FeatureExtractor("data")
extractor.data_loader.create_sample_data()
```

### Проверка признаков:
```python
features = extractor.extract_features()
stats = extractor.get_feature_statistics(features)
print(stats)
```

### Анализ важности признаков:
```python
from src.model_trainer import ModelTrainer
trainer = ModelTrainer("models")
trainer.load_model("antifraud_model_v1")
importance = trainer.get_feature_importance(top_n=10)
print(importance)
```

## 📦 Зависимости

- **pandas** - обработка данных
- **numpy** - численные вычисления
- **scikit-learn** - машинное обучение
- **librosa** - обработка аудио
- **pyarrow** - работа с Parquet
- **joblib** - сериализация моделей

## ⚡ Производительность

### Рекомендуемые характеристики:
- **RAM**: 8GB+
- **CPU**: 4+ ядра
- **Хранилище**: 5GB свободного места

### Время выполнения (примерно):
- **Обучение**: 5-15 минут (1000 образцов)
- **Предсказание**: 1-5 минут (100 образцов)
- **Извлечение признаков**: зависит от объема данных

## 🐛 Устранение неполадок

### Частые проблемы:

1. **Модуль не найден**:
   ```bash
   export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
   ```

2. **Нет данных**:
   - Система автоматически создаст тестовые данные
   - Проверьте пути к файлам

3. **Ошибка аудио**:
   - Установите: `pip install soundfile`
   - Проверьте формат файлов

4. **Низкое качество модели**:
   - Увеличьте размер обучающей выборки
   - Проверьте качество признаков
   - Настройте параметры модели

### Логи и отладка:
```bash
python scripts/main.py --check  # Проверка системы
python scripts/main.py --status # Статус компонентов
```

## 📄 Лицензия

MIT License - свободное использование для образовательных и коммерческих целей.

## 👥 Команда разработки

MVP команда хакатона - быстрая разработка антифрод решения для банковской сферы.

## 🔮 Планы развития

- Добавление новых типов признаков
- Ансамбль моделей
- Real-time обработка
- Web интерфейс
- API для интеграции
- Объяснимость предсказаний (SHAP)

---

**Готово к использованию!** 🚀

Для быстрого старта выполните:
```bash
python scripts/main.py --full
```
