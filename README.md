# 🛡️ MVP Антифрод система для банка

Система машинного обучения для детекции банковского мошенничества, разработанная для хакатона.

## 📋 Описание

Антифрод система анализирует поведенческие данные пользователей (Amplitude) и аудио данные для выявления потенциально мошеннических операций. Система использует машинное обучение для классификации транзакций как легитимных или подозрительных.

**Новое**: Интеграция с LLM (Large Language Models) для генерации понятных объяснений решений и автоматических отчетов.
- 🤖 **OpenAI API** - для продвинутых решений
- 🔒 **Локальная LLM** - бесплатно, приватно, без интернета (рекомендуется!)
- 📊 **Реальные данные** - полная поддержка ваших Parquet файлов и аудиозаписей

## 🏗️ Архитектура системы

```
hackathon/
├── data/                          # Данные
│   ├── amplitude/                 # Parquet файлы с поведенческими данными
│   ├── audio/                     # WAV аудио файлы
│   └── targets.csv               # Целевые метки (fraud/not fraud)
├── src/                          # Исходный код
│   ├── __init__.py
│   ├── data_loader.py            # Загрузка данных
│   ├── amplitude_processor.py    # Обработка Amplitude данных
│   ├── audio_processor.py        # Обработка аудио
│   ├── feature_extractor.py      # Объединение признаков
│   ├── model_trainer.py          # ML модель
│   ├── llm_enhancer.py           # 🤖 OpenAI LLM интеграция
│   ├── local_llm_enhancer.py     # 🔒 Локальная LLM интеграция
│   ├── real_data_loader.py       # 📊 Загрузчик реальных данных (NEW!)
│   └── real_features_processor.py # 📊 Процессор реальных признаков (NEW!)
├── scripts/                      # Скрипты запуска
│   ├── main.py                   # Главный скрипт
│   ├── train.py                  # Обучение модели
│   ├── predict.py                # Предсказания
│   ├── predict_with_llm.py       # 🧠 Предсказания с OpenAI LLM
│   ├── predict_local_llm.py      # 🔒 Предсказания с локальной LLM
│   ├── train_real_data.py        # 📊 Обучение на реальных данных (NEW!)
│   └── predict_real_data_with_llm.py # 📊 Анализ реальных данных с LLM (NEW!)
├── models/                       # Сохраненные модели
├── output/                       # Результаты
├── requirements.txt              # Зависимости
├── DATA_GUIDE.md                 # 📊 Руководство по данным
├── LLM_GUIDE.md                  # 🤖 Руководство по OpenAI LLM
├── LOCAL_LLM_SETUP.md            # 🔒 Руководство по локальной LLM
└── REAL_DATA_QUICK_START.md      # 📊 Быстрый старт с реальными данными (NEW!)
```

### 🤖 LLM Компоненты (Опционально)

**Базовая система (без LLM):**
```
Данные → Признаки → Random Forest → Предсказания
```

**Расширенная система (с LLM):**
```
Данные → Признаки → Random Forest → Предсказания → LLM → Объяснения + Отчеты
```

**🔥 Система для реальных данных:**
```
Ваши Parquet + Аудио → Реальные признаки → Обученная модель → LLM анализ → Готовые отчеты
```

**🔒 Локальная LLM (Рекомендуется!):**
- ✅ Бесплатно (никаких затрат на API)
- ✅ Приватность (данные не покидают систему)
- ✅ Автономность (работает без интернета)
- ✅ Контроль (полный контроль над моделью)

**🤖 OpenAI API LLM:**
- ⚡ Высокое качество объяснений
- 💰 Стоимость: $2-60 за 1000 объяснений
- 🌐 Требует интернет

**Возможности LLM (любой вариант):**
- 💬 Понятные объяснения решений
- 📊 Автоматические отчеты
- 🎯 Персонализированные рекомендации
- 📈 Анализ трендов мошенничества
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите ваши данные в соответствующие директории:
- **Amplitude данные**: `data/amplitude/*.parquet`
- **Аудио файлы**: `data/audio/*.wav`
- **Целевые метки**: `data/targets.csv` (колонки: user_id, is_fraud)

### 3. Запуск системы

#### Полный пайплайн (обучение + предсказание):
```bash
python scripts/main.py --full
```

#### Только обучение:
```bash
python scripts/main.py --train
```

#### Только предсказания:
```bash
python scripts/main.py --predict
```

#### Предсказания с локальной LLM (рекомендуется):
```bash
# Сначала установите Ollama: https://ollama.ai
ollama serve  # В отдельном терминале
ollama pull llama3.2:3b  # Загрузка модели (~6GB)

python scripts/predict_local_llm.py
```

#### 🔥 Работа с РЕАЛЬНЫМИ данными:
```bash
# 1. Разместите ваши файлы:
#    data/amplitude/train_amplitude_chunk_*.parquet
#    data/amplitude/train_app_data.parquet  
#    data/amplitude/train_target_data.parquet
#    data/audiofiles/*.wav

# 2. Обучение на ваших данных
python scripts/train_real_data.py

# 3. Анализ с локальной LLM
python scripts/predict_real_data_with_llm.py
```

#### Предсказания с OpenAI LLM:
```bash
export OPENAI_API_KEY="your-key"
python scripts/predict_with_llm.py
```

#### Проверка системы:
```bash
python scripts/main.py --check
```

## 📊 Компоненты системы

### DataLoader
- Загружает данные из Parquet файлов (Amplitude)
- Обрабатывает аудио файлы в формате WAV
- Загружает целевые метки из CSV

### AmplitudeProcessor
Извлекает поведенческие признаки:
- **Временные**: частота активности, время между событиями
- **Поведенческие**: типы событий, продолжительность сессий
- **Активность**: паттерны по дням/часам

### AudioProcessor
Извлекает аудио признаки:
- **MFCC**: Mel-frequency cepstral coefficients
- **Спектральные**: центроид, полоса пропускания, roll-off
- **Временные**: RMS энергия, основная частота

### FeatureExtractor
- Объединяет признаки из всех источников
- Обрабатывает пропущенные значения
- Создает единый датасет для ML

### ModelTrainer
- Использует Random Forest для классификации
- Автоматическая предобработка данных
- Валидация и метрики качества

### LLMEnhancer (🤖 Новый компонент)
- Генерирует объяснения решений модели
- Создает автоматические отчеты
- Анализирует паттерны мошенничества
- Предоставляет рекомендации по действиям

## 🎯 Результаты работы

После выполнения система создает:

### В директории `models/`:
- `antifraud_model_v1.joblib` - обученная модель
- `model_info.txt` - информация о модели и метриках
- `training_features.csv` - признаки обучающей выборки

### В директории `output/`:
- `fraud_predictions.csv` - детальные предсказания
- `prediction_summary.txt` - сводка результатов
- `recommendations.txt` - рекомендации по действиям

### С локальной LLM (дополнительно):
- `fraud_predictions_local_llm.csv` - предсказания + объяснения от локальной LLM
- `fraud_report_local_llm.txt` - детальный отчет с анализом
- `fraud_report_local_llm.html` - интерактивный HTML отчет

### 🔥 Для реальных данных:
- `real_data_predictions_with_llm.csv` - анализ ваших данных с LLM
- `real_data_fraud_report.txt` - детальный отчет по реальным случаям
- `real_data_fraud_analysis.html` - интерактивный анализ ваших данных
- `training_report.txt` - отчет об обучении на ваших данных

### С OpenAI LLM (дополнительно):
- `fraud_predictions_with_llm.csv` - предсказания + объяснения
- `detailed_fraud_report_llm.txt` - детальный отчет с LLM анализом
- `fraud_report_with_llm.html` - интерактивный HTML отчет

## 📈 Метрики качества

Система отслеживает следующие метрики:
- **AUC-ROC**: площадь под ROC-кривой
- **Precision**: точность детекции мошенничества
- **Recall**: полнота детекции мошенничества
- **F1-Score**: гармоническое среднее precision и recall

## 🔧 Конфигурация

### Основные параметры модели:
- **Random Forest**: n_estimators=100, max_depth=10
- **Частота дискретизации аудио**: 22050 Hz
- **MFCC коэффициентов**: 13
- **Размер тестовой выборки**: 20%

### Пороги риска:
- **Низкий риск**: < 30%
- **Средний риск**: 30-70%
- **Высокий риск**: > 70%

## 📋 Формат данных

### Amplitude данные (Parquet):
```
user_id | event_time | event_type | session_duration | click_count | page_views
user_1  | 2024-01-01 | login      | 300              | 10          | 5
```

### Целевые метки (CSV):
```
user_id,is_fraud
user_1,0
user_2,1
```

### Аудио файлы:
- Формат: WAV, MP3, FLAC
- Именование: `{user_id}_audio.wav`

## 🚨 Использование результатов

### Случаи высокого риска (≥70%):
1. **Немедленная проверка** транзакций
2. **Блокировка** подозрительных операций
3. **Контакт с клиентом** для подтверждения

### Случаи среднего риска (30-70%):
1. **Усиленный мониторинг**
2. **Дополнительные алерты**
3. **Периодический пересмотр**

## 🛠️ Разработка и отладка

### Создание тестовых данных:
```python
from src.feature_extractor import FeatureExtractor
extractor = FeatureExtractor("data")
extractor.data_loader.create_sample_data()
```

### Проверка признаков:
```python
features = extractor.extract_features()
stats = extractor.get_feature_statistics(features)
print(stats)
```

### Анализ важности признаков:
```python
from src.model_trainer import ModelTrainer
trainer = ModelTrainer("models")
trainer.load_model("antifraud_model_v1")
importance = trainer.get_feature_importance(top_n=10)
print(importance)
```

## 📦 Зависимости

### Базовые зависимости:
- **pandas** - обработка данных
- **numpy** - численные вычисления
- **scikit-learn** - машинное обучение
- **librosa** - обработка аудио
- **pyarrow** - работа с Parquet
- **joblib** - сериализация моделей

### Для локальной LLM (рекомендуется):
- **Ollama** - локальный сервер LLM (https://ollama.ai)
- **requests** - для API запросов к Ollama

### Для OpenAI LLM (опционально):
- **openai** - интеграция с GPT моделями

## ⚡ Производительность

### Рекомендуемые характеристики:
- **RAM**: 8GB+
- **CPU**: 4+ ядра
- **Хранилище**: 5GB свободного места

### Для локальной LLM (дополнительно):
- **RAM**: 8GB+ (для модели llama3.2:3b)
- **Хранилище**: +10GB (для модели)
- **GPU**: Опционально (NVIDIA для ускорения)

### Время выполнения (примерно):
- **Обучение**: 5-15 минут (1000 образцов)
- **Предсказание**: 1-5 минут (100 образцов)
- **Извлечение признаков**: зависит от объема данных

## 🐛 Устранение неполадок

### Частые проблемы:

1. **Модуль не найден**:
   ```bash
   export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
   ```

2. **Нет данных**:
   - Система автоматически создаст тестовые данные
   - Проверьте пути к файлам

3. **Ошибка аудио**:
   - Установите: `pip install soundfile`
   - Проверьте формат файлов

4. **Низкое качество модели**:
   - Увеличьте размер обучающей выборки
   - Проверьте качество признаков
   - Настройте параметры модели

### Логи и отладка:
```bash
python scripts/main.py --check  # Проверка системы
python scripts/main.py --status # Статус компонентов
```

## 📄 Лицензия

MIT License - свободное использование для образовательных и коммерческих целей.

## 👥 Команда разработки

MVP команда хакатона - быстрая разработка антифрод решения для банковской сферы.

## 🔮 Планы развития

- ✅ **LLM интеграция** - объяснения и отчеты
- Добавление новых типов признаков
- Ансамбль моделей
- Real-time обработка
- Web интерфейс
- API для интеграции
- Улучшенная объяснимость (SHAP + LLM)

---

**Готово к использованию!** 🚀

Для быстрого старта выполните:

### 🧪 Демо с тестовыми данными:
```bash
# Базовая система (без LLM)
python scripts/main.py --full

# С локальной LLM (рекомендуется!)
# 1. Установите Ollama: curl -fsSL https://ollama.ai/install.sh | sh
# 2. Запустите: ollama serve
# 3. Загрузите модель: ollama pull llama3.2:3b
# 4. Запустите:
python scripts/predict_local_llm.py
```

### 🔥 Ваши реальные данные:
```bash
# 1. Установите Ollama (как выше)
# 2. Разместите файлы согласно REAL_DATA_QUICK_START.md
# 3. Обучите на ваших данных:
python scripts/train_real_data.py
# 4. Анализируйте с LLM:
python scripts/predict_real_data_with_llm.py
```

### 💰 С OpenAI LLM (требует API ключ):
```bash
export OPENAI_API_KEY="your-key"
python scripts/predict_with_llm.py
```

## 📚 Дополнительная документация

- 🔥 **[REAL_DATA_QUICK_START.md](REAL_DATA_QUICK_START.md)** - Быстрый старт с ВАШИМИ данными!
- 📊 **[DATA_GUIDE.md](DATA_GUIDE.md)** - Подробное руководство по подключению данных
- 🔒 **[LOCAL_LLM_SETUP.md](LOCAL_LLM_SETUP.md)** - Установка локальной LLM (рекомендуется!)
- 🤖 **[LLM_GUIDE.md](LLM_GUIDE.md)** - Интеграция с OpenAI LLM
- 🧪 **prepare_data.py** - Скрипт подготовки и валидации данных

## 📊 Ваши реальные данные
Система полностью адаптирована для работы с реальными банковскими данными:
- ✅ Parquet файлы с техническими данными звонков
- ✅ Аудиозаписи в формате WAV/MP3
- ✅ Справочные данные по заявкам
- ✅ Целевые метки мошенничества
- ✅ Автоматическое извлечение признаков
- ✅ Объяснения решений через локальную LLM
